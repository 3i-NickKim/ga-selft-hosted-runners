name : self-hosted-mac-pro

on:
  push:
    branches:
      - 'main'
      - 'master'

jobs:
  build:
    runs-on: self-hosted

    steps:
      - name: hello world
        run: |
          echo Add other actions to build,
          echo test  , and deploy your project.
